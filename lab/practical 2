#include<stdio.h>
#include<fcntl.h>
#include<unistd.h>

int main()
{
    char ch;
    int f1=open("a.txt",O_RDONLY);
    int f2=open("b.txt",O_CREAT|O_WRONLY,0644);

    while(read(f1,&ch,1)>0)
        write(f2,&ch,1);

    close(f1);
    close(f2);

    printf("File Copied");
    return 0;
}
